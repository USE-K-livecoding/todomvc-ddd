import React from "react";

const Application: React.FC = (props) => {
    console.log(props);
    return (
        <React.Fragment>
            Hello, world.
        </React.Fragment>
    )
};

export default Application;
